# ParaEQ → Gumroad: release manifest (binding #1 of Convey's generic release engine).
# The mechanism lives in ~/work/convey/release/; this file is the whitelabel — every
# product-specific value the release needs. Run:
#   ~/work/convey/release/release.sh ~/work/paraeq/release.conf
#   DRY_RUN=1 ~/work/convey/release/release.sh ~/work/paraeq   (preview, mutate nothing)

service=gumroad

# ── the Gumroad product (the <product>) ──
gumroad.product_id=-QuWS2I6mnM78PysotOKqw==
gumroad.url=https://lackluster.gumroad.com/l/joucal
gumroad.name=ParaEQ 301
gumroad.price=135.40
gumroad.currency=eur
gumroad.tags=eq, audio plugin, vst3, au, mixing, saturation
gumroad.description_html=features/paraeq.listing.html
gumroad.landing_html=landing.html

# ── how THIS product builds (product-specific; the engine just runs these) ──
build.dir=/Users/esaruoho/work/paraeq
build.build=./start.sh
build.package=SKIP_BUILD=1 ./package.sh
build.snapshot=./scripts/snap-ui.sh
build.zip_from=dist/LATEST_ZIP.txt
build.shot_from=dist/LATEST_SHOT.txt
